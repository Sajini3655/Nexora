# Frontend environment variables (copy to .env.local for local development)
# Set this to your deployed backend URL (e.g. Railway) in Vercel's environment settings
VITE_API_BASE_URL=https://your-railway-backend-url.up.railway.app
